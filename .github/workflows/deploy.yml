name: Deploy to Render

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy
        run: curl -s --max-time 30 "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
