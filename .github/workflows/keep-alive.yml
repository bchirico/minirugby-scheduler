name: Keep Render alive

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s --max-time 30 -o /dev/null -w "%{http_code}" "${{ secrets.RENDER_APP_URL }}"
