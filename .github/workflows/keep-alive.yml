name: Keep Render alive

on:
  schedule:
    - cron: "*/14 * * * *"  # every 14 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s -o /dev/null -w "%{http_code}" "${{ secrets.RENDER_APP_URL }}"
